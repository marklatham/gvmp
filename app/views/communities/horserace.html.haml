- @page_title = @community.short_name +  ": Horserace"

- start_time = Time.now

%h2{:style => "text-align: center"}
  = @community.name + ':'
  Media Horserace

%h3{:style => "text-align: center"}
  Past funding shares from
  = link_to "this ballot", "/" + @community.idstring
  - if @yearly_rankings.any?
    for websites/blogs with positive
    -# = @master_rankings[0].start.year.to_s
    shares

- if @yearly_rankings.any?
  = render :partial => 'horserace_table'
- else
  %p{:style => "text-align: center"} [There are no past funding shares from this ballot yet.]

- if current_user and (current_user.permission >= 7.0)
  %p.permission
    Page prep time was:
    = Time.now - start_time
    seconds.

%br