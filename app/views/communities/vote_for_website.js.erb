jQuery("<%="#vote_element_#{@website.id}"%>").before("<%= escape_javascript(render(:partial => 'vote', :locals => { :community => @community, :website => @website, :support => @support, :flag => 1 } )) %>").remove();

jQuery("<%="#vote_element_#{@website.id}"%>").ready(function() {
  ajaxLinks();
});

